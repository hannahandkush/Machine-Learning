# Pip-only extras not packaged on conda-forge (or where a newer release is
# needed). Installed inside the `veg-s2s` conda env — see environment.yml.
#
# Leave this empty/minimal: prefer adding packages to environment.yml so
# GDAL-dependent libraries are resolved by conda, not pip.

# Deep learning — via pip so each platform gets the right wheel
# (MPS on Apple Silicon, CUDA on the HPC) without conda channel juggling.
torch>=2.2
torchvision>=0.17
segmentation-models-pytorch>=0.5

# Required by the Swin-YNet inference package (models/updated_model/bacdm_predict).
einops>=0.7
timm>=0.9
